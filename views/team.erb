<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title>Basketball Team</title>
 </head>
 <body>
   <% params.each do |key, value| %>
      <% if key == "Team Name" %>
            <h1><%= "#{key}: #{value}" %></h2>
      <% else %>
            <h2><%= "#{key}: #{value}" if key != "submit" %></h2>
      <% end %>
   <% end %>

 </body>
</html>
