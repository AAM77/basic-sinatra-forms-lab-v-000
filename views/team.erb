<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title>Basketball Team</title>
 </head>
 <body>
   <% params.each do |key, value| %>
        <h1><%= "#{key}: #{value}" if key == 'name' %></h2>
      <% else %>
            <h2><%= "#{key}: #{value}" if key != "submit" %></h2>
      <% end %>
   <% end %>

 </body>
</html>
